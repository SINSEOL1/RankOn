name: Build

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Restore
        run: dotnet restore src/RankOn/RankOn.csproj

      - name: Build
        run: dotnet build src/RankOn/RankOn.csproj --configuration Release --no-restore

      - name: Publish
        run: >
          dotnet publish src/RankOn/RankOn.csproj
          --configuration Release
          --runtime win-x64
          --self-contained true
          -p:PublishSingleFile=true
          -p:IncludeNativeLibrariesForSelfExtract=true
          --output publish

      - name: Install Inno Setup
        run: choco install innosetup --no-progress -y

      - name: Build test installer
        shell: pwsh
        run: |
          & "${env:ProgramFiles(x86)}\Inno Setup 6\ISCC.exe" "/DMyAppVersion=0.9.0" "installer\RankOn.iss"

      - name: Upload installer
        uses: actions/upload-artifact@v4
        with:
          name: RankOn-Windows-test
          path: installer/output/RankOn-Setup-*.exe

      - name: Upload portable build
        uses: actions/upload-artifact@v4
        with:
          name: RankOn-Windows-portable
          path: publish/
